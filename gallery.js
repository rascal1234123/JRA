(()=>{const data=Array.isArray(window.JRA_GALLERY_DATA)?window.JRA_GALLERY_DATA:[];const grid=document.querySelector('[data-gallery-grid]');if(!grid)return;const count=document.querySelector('[data-gallery-count]');const more=document.querySelector('[data-gallery-more]');const empty=document.querySelector('[data-gallery-empty]');const lightbox=document.querySelector('[data-gallery-lightbox]');let publisher='all',type='all',shown=0,current=-1,visible=[];const batch=36;const filtered=()=>data.filter(item=>(publisher==='all'||item.category===publisher)&&(type==='all'||item.type===type));const tile=item=>{const article=document.createElement('article');article.className='gallery-tile';const button=document.createElement('button');button.type='button';button.setAttribute('aria-label',`View ${item.title}`);button.innerHTML=`<img src="${item.thumb}" alt="${item.title}" loading="lazy" decoding="async"><span class="gallery-tile-copy"><strong>${item.title}</strong><span>${item.category} · ${item.type}</span></span>`;button.addEventListener('click',()=>openItem(visible.indexOf(item)));article.append(button);return article};const render=(reset=false)=>{visible=filtered();if(reset){shown=0;grid.replaceChildren()}const next=visible.slice(shown,shown+batch);next.forEach(item=>grid.append(tile(item)));shown+=next.length;more.hidden=shown>=visible.length;empty.hidden=visible.length>0;count.textContent=`${visible.length} curated artwork${visible.length===1?'':'s'}`};const setButtons=(root,attr,value)=>{root?.querySelectorAll('button').forEach(b=>b.classList.toggle('active',b.getAttribute(attr)===value))};const filters=document.querySelector('[data-gallery-filters]');filters?.addEventListener('click',e=>{const b=e.target.closest('[data-gallery-filter]');if(!b)return;publisher=b.dataset.galleryFilter;setButtons(filters,'data-gallery-filter',publisher);render(true)});const types=document.querySelector('[data-gallery-types]');types?.addEventListener('click',e=>{const b=e.target.closest('[data-gallery-type]');if(!b)return;type=b.dataset.galleryType;setButtons(types,'data-gallery-type',type);render(true)});more?.addEventListener('click',()=>render(false));const image=lightbox?.querySelector('[data-lightbox-image]');const title=lightbox?.querySelector('[data-lightbox-title]');const meta=lightbox?.querySelector('[data-lightbox-meta]');const show=()=>{const item=visible[current];if(!item)return;image.src=item.full;image.alt=item.title;title.textContent=item.title;meta.textContent=`${item.category} · ${item.type}`};const openItem=index=>{current=index;show();lightbox.hidden=false;document.body.classList.add('gallery-lightbox-open');lightbox.querySelector('[data-lightbox-close]')?.focus()};const close=()=>{lightbox.hidden=true;document.body.classList.remove('gallery-lightbox-open')};const move=step=>{if(!visible.length)return;current=(current+step+visible.length)%visible.length;show()};lightbox?.querySelector('[data-lightbox-close]')?.addEventListener('click',close);lightbox?.querySelector('[data-lightbox-prev]')?.addEventListener('click',()=>move(-1));lightbox?.querySelector('[data-lightbox-next]')?.addEventListener('click',()=>move(1));lightbox?.addEventListener('click',e=>{if(e.target===lightbox)close()});addEventListener('keydown',e=>{if(lightbox?.hidden)return;if(e.key==='Escape')close();if(e.key==='ArrowLeft')move(-1);if(e.key==='ArrowRight')move(1)});render(true)})();