const intro=document.querySelector('[data-intro]');const skipIntro=document.querySelector('[data-skip-intro]');const forceIntro=new URLSearchParams(location.search).get('intro')==='1';let introTimers=[];const clearIntroTimers=()=>{introTimers.forEach(clearTimeout);introTimers=[]};const finishIntro=()=>{if(!intro)return;clearIntroTimers();intro.classList.add('finished');document.body.classList.remove('intro-pending');sessionStorage.setItem('jraIntroSeen','1');setTimeout(()=>intro.remove(),900)};const runIntro=()=>{if(!intro)return;const logo=intro.querySelector('.intro-logo');const shade=intro.querySelector('.intro-shade');while(intro.querySelectorAll('.intro-slide').length<9){const slide=document.createElement('div');slide.className='intro-slide';slide.setAttribute('aria-hidden','true');intro.insertBefore(slide,shade)}const slides=[...intro.querySelectorAll('.intro-slide')].slice(0,9);const settings=[{size:'cover',position:'center',from:'scale(1.02)',to:'scale(1.09)'},{size:'contain',position:'center 38%',from:'scale(1.03) translateY(1.5%)',to:'scale(1.08) translateY(-1.5%)'},{size:'contain',position:'center',from:'scale(1.04) translateX(1.5%)',to:'scale(1.09) translateX(-1.5%)'},{size:'contain',position:'center',from:'scale(1.03) translate(1%,1%)',to:'scale(1.08) translate(-1%,-1%)'},{size:'contain',position:'center 28%',from:'scale(1.02) translateY(1%)',to:'scale(1.07) translateY(-1%)'},{size:'contain',position:'center',from:'scale(1.03)',to:'scale(1.09)'},{size:'contain',position:'center',from:'scale(1.04) translateX(-1.5%)',to:'scale(1.09) translateX(1.5%)'},{size:'contain',position:'center',from:'scale(1.03)',to:'scale(1.09)'},{size:'contain',position:'center',from:'scale(1.03) translateY(1%)',to:'scale(1.08) translateY(-1%)'}];slides.forEach((slide,index)=>{const config=settings[index];slide.style.animation='none';slide.style.opacity='0';slide.style.transform=config.from;slide.style.transition='opacity .7s ease, transform 2.7s ease';slide.style.backgroundImage=`url('intro-${index+1}.jpg?v=20260725-4')`;slide.style.backgroundSize=config.size;slide.style.backgroundPosition=config.position;slide.style.backgroundRepeat='no-repeat';slide.style.backgroundColor='#050505'});if(logo){logo.style.animation='none';logo.style.opacity='1';logo.style.transition='opacity .65s ease'}const showSlide=index=>{slides.forEach((slide,i)=>{slide.style.opacity=i===index?'1':'0';if(i===index)slide.style.transform=settings[i].to;else slide.style.transform=settings[i].from});if(logo)logo.style.opacity='0'};const start=1700;const interval=2500;slides.forEach((_,index)=>introTimers.push(setTimeout(()=>showSlide(index),start+index*interval)));introTimers.push(setTimeout(()=>{slides.forEach(slide=>slide.style.opacity='0');if(logo)logo.style.opacity='1'},start+slides.length*interval));introTimers.push(setTimeout(finishIntro,26000))};if(intro){const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const seen=sessionStorage.getItem('jraIntroSeen')==='1';if(reduced||seen&&!forceIntro){finishIntro()}else{if(forceIntro)sessionStorage.removeItem('jraIntroSeen');skipIntro?.addEventListener('click',finishIntro);runIntro()}}const header=document.querySelector('[data-header]');const menu=document.querySelector('[data-menu-button]');const nav=document.querySelector('[data-nav]');addEventListener('scroll',()=>header?.classList.toggle('scrolled',scrollY>20));menu?.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));nav?.classList.toggle('open',!open)});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu?.setAttribute('aria-expanded','false')}));document.querySelectorAll('[data-filter]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach(b=>b.classList.remove('active'));button.classList.add('active');const filter=button.dataset.filter;document.querySelectorAll('[data-category]').forEach(card=>card.hidden=filter!=='all'&&card.dataset.category!==filter)}));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>entry.target.classList.toggle('visible',entry.isIntersecting)),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));const year=document.querySelector('[data-year]');if(year)year.textContent=new Date().getFullYear();